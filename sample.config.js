var config = {};

config.title = 'Trello Report Generator';
config.organization = 'organization-title';
config.key = 'my-secret-key';
config.token = 'my-secret-token';

config.boards = [
    {
        name : 'EXEMPLE',
        id: 'boardid'
    }
];

module.exports = config;
